/** @type {const} */
const themeColors = {
  // Cyberpunk color palette
  primary: { light: '#A020F0', dark: '#A020F0' }, // Electric Purple
  background: { light: '#000000', dark: '#000000' }, // Pure Black
  surface: { light: '#1A1A2E', dark: '#1A1A2E' }, // Dark Gray
  foreground: { light: '#FFFFFF', dark: '#FFFFFF' }, // White
  muted: { light: '#888888', dark: '#888888' }, // Gray
  border: { light: '#00D9FF', dark: '#00D9FF' }, // Cyber Blue
  success: { light: '#00FF00', dark: '#00FF00' }, // Neon Green
  warning: { light: '#FFD700', dark: '#FFD700' }, // Neon Yellow
  error: { light: '#FF0080', dark: '#FF0080' }, // Neon Pink
  accent: { light: '#00D9FF', dark: '#00D9FF' }, // Cyber Blue
};

module.exports = { themeColors };
