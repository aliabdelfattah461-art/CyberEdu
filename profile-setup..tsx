import React, { useState } from 'react';\nimport { View, Text, TextInput, TouchableOpacity, ScrollView, Alert } from 'react-native';\nimport { useRouter } from 'expo-router';\nimport { ScreenContainer } from '@/components/screen-container';\nimport AsyncStorage from '@react-native-async-storage/async-storage';\n\nexport default function ProfileSetupScreen() {\n  const [alias, setAlias] = useState('');\n  const [vodafoneCash, setVodafoneCash] = useState('');\n  const [loading, setLoading] = useState(false);\n  const router = useRouter();\n\n  const handleCompleteSetup = async () => {\n    if (!alias.trim() || !vodafoneCash.trim()) {\n      Alert.alert('خطأ', 'يرجى ملء جميع الحقول');\n      return;\n    }\n\n    if (!/^01[0-2]\\d{8}$/.test(vodafoneCash)) {\n      Alert.alert('خطأ', 'يرجى إدخال رقم فودافون كاش صحيح');\n      return;\n    }\n\n    setLoading(true);\n    try {\n      // Save profile data\n      await AsyncStorage.setItem('user_alias', alias);\n      await AsyncStorage.setItem('vodafone_cash', vodafoneCash);\n      await AsyncStorage.setItem('user_authenticated', 'true');\n      \n      // Navigate to home\n      router.replace('/(tabs)');\n    } catch (error) {\n      Alert.alert('خطأ', 'حدث خطأ أثناء حفظ البيانات');\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  return (\n    <ScreenContainer className=\"flex-1 bg-black\">\n      <ScrollView contentContainerStyle={{ flexGrow: 1 }} className=\"p-6\">\n        <View className=\"flex-1 justify-center gap-8\">\n          {/* Header */}\n          <View className=\"items-center gap-2 mb-4\">\n            <Text className=\"text-3xl font-bold text-primary\">\n              إعداد البروفايل\n            </Text>\n            <Text className=\"text-sm text-border\">\n              أكمل بيانات حسابك\n            </Text>\n          </View>\n\n          {/* Form Fields */}\n          <View className=\"gap-6\">\n            {/* Dark Web Alias */}\n            <View>\n              <Text className=\"text-foreground text-sm font-semibold mb-2\">\n                اسمك الحركي على الدارك ويب\n              </Text>\n              <TextInput\n                value={alias}\n                onChangeText={setAlias}\n                placeholder=\"أدخل اسماً مستعاراً\"\n                placeholderTextColor=\"#666\"\n                editable={!loading}\n                className=\"bg-surface border border-border rounded-lg p-4 text-foreground text-base\"\n              />\n              <Text className=\"text-muted text-xs mt-2\">\n                هذا الاسم سيظهر في الدردشات والمجتمعات\n              </Text>\n            </View>\n\n            {/* Vodafone Cash */}\n            <View>\n              <Text className=\"text-foreground text-sm font-semibold mb-2\">\n                رقم فودافون كاش\n              </Text>\n              <TextInput\n                value={vodafoneCash}\n                onChangeText={setVodafoneCash}\n                placeholder=\"01xxxxxxxxx\"\n                placeholderTextColor=\"#666\"\n                keyboardType=\"phone-pad\"\n                editable={!loading}\n                className=\"bg-surface border border-border rounded-lg p-4 text-foreground text-base\"\n              />\n              <Text className=\"text-muted text-xs mt-2\">\n                سيتم استخدام هذا الرقم لصرف الأرباح\n              </Text>\n            </View>\n          </View>\n\n          {/* Complete Button */}\n          <TouchableOpacity\n            onPress={handleCompleteSetup}\n            disabled={loading}\n            className={`py-4 rounded-lg items-center mt-4 ${\n              loading ? 'opacity-50' : 'opacity-100'\n            }`}\n            style={{\n              backgroundColor: '#A020F0',\n              shadowColor: '#A020F0',\n              shadowOffset: { width: 0, height: 0 },\n              shadowOpacity: 0.5,\n              shadowRadius: 10,\n            }}\n          >\n            <Text className=\"text-background font-bold text-lg\">\n              {loading ? 'جاري الإعداد...' : 'إكمال الإعداد'}\n            </Text>\n          </TouchableOpacity>\n        </View>\n      </ScrollView>\n    </ScreenContainer>\n  );\n}\n
